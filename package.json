{
  "private": true,
  "name": "storybook-addon-mock",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "yarn workspace storybook-addon-mock clean",
    "build": "yarn workspace storybook-addon-mock clean",
    "lint": "yarn workspaces lint:fix",
    "test": "yarn workspaces test",
    "ci": "yarn run lint && yarn run test && yarn run build",
    "dedup": "yarn-deduplicate"
  },
  "devDependencies": {
    "eslint": "^7.22.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-jsx-a11y": "^6.4.1",
    "eslint-plugin-prettier": "^3.4.0",
    "eslint-plugin-react": "^7.23.1",
    "prettier": "^2.3.2",
    "yarn-deduplicate": "^3.1.0"
  },
  "resolutions": {
    "**/postcss": "^8.2.13",
    "**/prismjs": "^1.25.0"
  }
}
